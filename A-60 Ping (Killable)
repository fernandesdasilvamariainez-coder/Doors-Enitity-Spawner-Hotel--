	local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/fernandesdasilvamariainez-coder/Aaaaaaa/refs/heads/main/Old%20Vynixu%20Entity%20Spawner"))() 
-- Create entity
local entityTable = Creator.createEntity({
    CustomName = "Unknown", -- Custom name of your entity
    Model = "rbxassetid://139692532333823", -- Can be GitHub file or rbxassetid
    Speed = 220, -- Percentage, 100 = default Rush speed
    DelayTime = 5, -- Time before starting cycles (seconds)
    HeightOffset = -1,
    CanKill = true,
    KillRange = 70,
    BreakLights = true,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled/Disabled
        8, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 3,
        WaitTime = 1,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {40, 50, 0.1, 1}, -- Shake values (don't change if you don't know)
        90, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://0", -- Image1 url
            Image2 = "rbxassetid://0", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 0, 0), -- Color
            },
            Tease = {
                false, -- Enabled/Disabled
                Min = 2,
                Max = 4,
            },
        },
    },
    CustomDialog = {}, -- Custom death message
})

-----[[ Advanced ]]-----
entityTable.Debug.OnEntitySpawned = function(entityTable)
    print("Entity has spawned:", entityTable.Model)
end

entityTable.Debug.OnEntityDespawned = function(entityTable)
	if getgenv().death == true then
		getgenv().Title = "Out of many Rebounds" --Title Here
		getgenv().Description = "Might come back or not" --Description Here
		getgenv().Reason = "Encounter and survive the rare Entity called A-60" --Reason Here
		getgenv().BadgeId = 11395251069  --Replace Number with Your Badge ID
		getgenv().Category = 10 --You can replace the Category or dont

		local Unlock = require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Lobby.RemoteListener.Modules.AchievementUnlock)
		local Achievements = debug.getupvalue(Unlock, 1)
		for i,v in pairs(require(game:GetService("ReplicatedStorage").Achievements)) do
			v.Title = getgenv().Title
			v.Desc = getgenv().Description
			v.Reason = getgenv().Reason
			v.BadgeId = getgenv().BadgeId
			v.Category = getgenv().Category
		end
		Unlock(nil,"Join")
	end
end

entityTable.Debug.OnEntityStartMoving = function(entityTable)
    print("Entity has started moving:", entityTable.Model)
end

entityTable.Debug.OnEntityFinishedRebound = function(entityTable)
    print("Entity has finished rebound:", entityTable.Model)
end

entityTable.Debug.OnEntityEnteredRoom = function(entityTable, room)
    print("Entity:", entityTable.Model, "has entered room:", room)
end

entityTable.Debug.OnLookAtEntity = function(entityTable)
    print("Player has looked at entity:", entityTable.Model)
end

entityTable.Debug.OnDeath = function(entityTable)
    local function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end	
    local JumpscareSound = GetGitSound("https://github.com/fernandesdasilvamariainez-coder/Repository/blob/main/A-60TrollSoundNotLoop.mp3?raw=true","DamageNoise") JumpscareSound.Parent = workspace
			JumpscareSound.Volume = 0.7
			JumpscareSound:Play() 

end
------------------------

-- Run the created entity
Creator.runEntity(entityTable)